node_modules/
*.log
.env
.env.local
coverage/
.DS_Store
*.tsbuildinfo
research/
.claude/

# `dist/` IS committed so `npm i -g github:...` works without a build step.
# Source-maps stay local to keep the repo lean.
dist/*.map
